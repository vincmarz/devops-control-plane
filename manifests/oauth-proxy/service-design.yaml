apiVersion: v1
kind: Service
metadata:
  name: devops-control-plane
  namespace: devops-control-plane
  labels:
    app.kubernetes.io/name: devops-control-plane
  annotations:
    devops-control-plane.phase: "9.3.8-design-only"
    devops-control-plane.note: "Template showing target Service port for OAuth Proxy; do not apply directly without review."
spec:
  ports:
    - name: https
      port: 8443
      protocol: TCP
      targetPort: oauth-proxy
